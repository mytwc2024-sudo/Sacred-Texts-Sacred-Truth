name: Oracle Shadow Runtime Patch

on:
  push:
    branches:
      - "agent/oracle-stage-2-2-shadow-runtime"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Apply guarded shadow-runtime patch
        run: python .github/scripts/apply-oracle-shadow-runtime-patch.py

      - name: Format changed Oracle sources
        run: deno fmt supabase/functions/oracle-query/index.ts supabase/functions/_shared/oracle-query-plan-shadow-runtime.ts supabase/functions/_shared/oracle-query-plan-shadow-runtime.test.ts

      - name: Run full Oracle contract tests
        run: deno test supabase/functions/_shared/oracle-contract.test.ts supabase/functions/_shared/oracle-evidence-relationship.test.ts supabase/functions/_shared/oracle-query-plan.test.ts supabase/functions/_shared/oracle-query-plan-integration.test.ts supabase/functions/_shared/oracle-query-plan-shadow.test.ts supabase/functions/_shared/oracle-query-plan-shadow-runtime.test.ts

      - name: Commit guarded runtime patch
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No patch diff to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add supabase/functions/oracle-query/index.ts supabase/functions/_shared/oracle-query-plan-shadow-runtime.ts supabase/functions/_shared/oracle-query-plan-shadow-runtime.test.ts
          git commit -m "Wire opt-in Stage 2.2 query-plan shadow runtime"
          git push origin HEAD:agent/oracle-stage-2-2-shadow-runtime
